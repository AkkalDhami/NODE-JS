<nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex items-center space-x-4">
                <span class="text-xl font-bold text-blue-600">📝 NotesApp</span>
                <a href="/"  class="hover:text-blue-600 <%= currentPage === 'index'  ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-700' %>">Home</a>
                <a href="my-notes" class=" <%= currentPage === 'my-notes' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-700'  %> hover:text-blue-600">My Notes</a>
            </div>
            <!-- Mobile Hamburger -->
            <div class="flex items-center md:hidden">
                <button id="menuBtn" class="text-gray-700 hover:text-blue-600 focus:outline-none">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>

</nav>