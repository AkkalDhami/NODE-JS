<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Notes App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
  <!-- 🌐 Navbar -->
  <%- include('../partials/nav.ejs') %>

    <!-- 📋 Note Form -->
    <main class="py-10 max-w-7xl mx-auto">
      <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 md:grid-cols-3 px-4 sm:px-6 lg:px-8">

        <% if (notes.length===0) { %>
          <div class="p-6 bg-zinc-50 ">
            <h3 class="text-xl font-bold text-gray-800 mb-2">
              📌 No notes found
            </h3>
          </div>
          <% } %>
            <% notes.forEach(note=> { %>
              <div class="p-6 bg-zinc-50 rounded-lg hover:shadow-lg duration-200">
                <h3 class="text-xl font-bold text-gray-800 mb-2">
                  📌 <%= note.title %>
                </h3>
                <p class="text-gray-600 text-sm leading-relaxed">
                  <%= note.content %>
                </p>

                <div class="flex justify-between items-center mt-4 text-sm text-gray-500">
                  <span>🗓️ <%= new Date(note.createdAt).toLocaleDateString("en-US", { month: "short" , day: "numeric" ,
                      year: "numeric" , }); %></span>
                  <!-- <span>🗓️ Jun 8, 2025</span> -->
                  <div class="space-x-2">
                    <a href="/edit-note/<%= note.id %>" class="text-blue-500 hover:underline">Edit</a>
                    <a href="/delete-note/<%= note.id %>" class="text-red-500 hover:underline">Delete</a>
                  </div>
                </div>
              </div>
              <% }) %>
      </div>
    </main>
</body>

</html>