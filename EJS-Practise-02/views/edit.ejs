<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="flex flex-col p-10 h-screen">
      <a href="/" class="text-orange-500 hover:underline mb-4 inline-block"
        >Go back to home</a
      >

      <form
        action="/edit"
        method="POST"
        class="flex flex-col space-y-4 w-full max-w-md"
      >
        <input
          type="text"
          name="title"
          value="<%= title %>"
          class="p-2 rounded bg-zinc-800 text-white border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-orange-500"
          readonly
        />
       <div class="flex relative justify-between items-center">
            <input
              type="text"
              name="newFilename"
              class="p-2 rounded w-full bg-zinc-800 text-white border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-orange-500 pr-10"
              placeholder="Enter new Filename"
              required
            /> <p class="absolute right-2 text-zinc-400">.txt</p>
       </div>

        <textarea
          name="newDescription"
          placeholder="Update Description"
          class="p-2 rounded bg-zinc-800 text-white border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-orange-500 resize-none h-32"
          required
        ></textarea>
        <button
          type="submit"
          class="p-2 rounded bg-yellow-500 text-white hover:bg-yellow-600 cursor-pointer focus:outline-none focus:ring-2 focus:ring-yellow-500"
        >
          Update
        </button>
      </form>
    </div>
  </body>
</html>
