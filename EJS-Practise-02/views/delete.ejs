<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="flex flex-col p-10 h-screen">
      <a href="/" class="text-orange-500 hover:underline mb-4 inline-block"
        >Go back to home</a
      >

      <form
        action="/delete"
        method="POST"
        class="flex flex-col space-y-4 w-full max-w-md"
      >
        <input
          type="text"
          name="deleteFilename"
          value="<%= title %>"
          class="p-2 rounded bg-zinc-800 text-white border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-orange-500"
          readonly
        />
        <button
          type="submit"
          class="p-2 rounded bg-red-500 text-white hover:bg-red-600 cursor-pointer focus:outline-none focus:ring-2 focus:ring-red-500"
        >
          Delete
        </button>
      </form>
    </div>
  </body>
</html>
